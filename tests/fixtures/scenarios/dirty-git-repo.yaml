name: dirty-git-repo
description: Current CLI smoke scenario for the dirty git repository input.
input: dirty-git-repo
args:
  - --version
expect:
  exit_code: 0
  stdout_contains:
    - 0.1.0
  stderr_empty: true
  files_unchanged_from_input: true
