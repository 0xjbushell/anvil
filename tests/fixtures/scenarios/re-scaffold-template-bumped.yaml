name: re-scaffold-template-bumped
description: Setup-backed re-scaffold scenario proving dry-run UPDATE classification for an older template.
input: re-scaffold-template-bumped
language: typescript
args:
  - init
  - --lang
  - typescript
  - --non-interactive
  - --dry-run
expect:
  exit_code: 0
  stdout_contains:
    - "Dry run: no files written."
    - "Files to update: 1"
  files_unchanged_after_setup: true
