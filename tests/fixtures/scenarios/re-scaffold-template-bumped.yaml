name: re-scaffold-template-bumped
description: Current CLI smoke scenario for the template-bumped re-scaffold input.
input: re-scaffold-template-bumped
args:
  - --version
expect:
  exit_code: 0
  stdout_contains:
    - 0.1.0
  stderr_empty: true
  files_unchanged_from_input: true
