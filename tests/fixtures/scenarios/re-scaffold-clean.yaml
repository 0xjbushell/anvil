name: re-scaffold-clean
description: Setup-backed re-scaffold scenario proving a clean TypeScript project is an idempotent no-op.
input: re-scaffold-clean
language: typescript
args:
  - init
  - --lang
  - typescript
  - --non-interactive
expect:
  exit_code: 0
  stdout_contains:
    - "Files created: 0"
    - "Files skipped: 0"
  files_unchanged_after_setup: true
